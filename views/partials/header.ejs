<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | Collaborative Story App</title>
    <meta name="description" content="Build a story by collaborating with multiple people">
    <meta name="author" content="Aaron Topping">

    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <nav>
            <div>
                <a href="/">Home</a>
                <% if (currentUser) { %>
                    <a href="/stories">Stories</a>
                <% } %>
            </div>
            <div>
                <% if (currentUser) { %>
                    <span>Welcome, <%= currentUsername %>!</span>
                    <a href="/auth/logout">Logout</a>
                <% } else { %>
                    <a href="/auth/login">Login</a>
                    <a href="/auth/register">Register</a>
                <% } %>
            </div>
        </nav>
    </header>
    <main class="container">